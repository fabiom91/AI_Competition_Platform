# Upper bounds are precautionary: the application was written and run against the
# 2022-era majors of these libraries and has not been tested against later ones.
# For an exact, reproducible set, run `pip freeze` inside a successful build and
# commit the result alongside this file.
Flask >= 2.0.2, < 3
pandas >= 1.3.4, < 2
numpy >= 1.21.3, < 2
firebase-admin >= 5.0.3, < 6
scikit-learn >= 1.0, < 2
pypandoc >= 1.6.4, < 2
retrying >= 1.3.3, < 2
flask-cors >= 3.0.10, < 4
gunicorn >= 20.1.0, < 21
